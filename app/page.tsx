import AppClient from "@/components/AppClient";

export default function Page() {
  // ear_outline.svg 的 viewBox（你文件里就是这个）
  const viewBox = "0 0 744.09 1052.4";
  return <AppClient viewBox={viewBox} />;
}
